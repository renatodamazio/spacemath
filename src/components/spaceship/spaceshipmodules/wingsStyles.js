import styled from "styled-components";

const size = 30;

export const Wing = styled.span`
    border: ${size}px solid #fff;
    height: ${size}px;
    width: ${size*2}px;
    display: inline-block;
    border-top-color: transparent;
    border-left-color: transparent;
`